<h2>Edit Shift</h2>

<%= render "form.html", Map.put(assigns, :action, event_weekend_day_shift_path(@conn, :update, @event_id, @weekend_id, @day_id, @shift)) %>

<span><%= link "Back", to: event_weekend_day_shift_path(@conn, :index, @event_id, @weekend_id, @day_id) %></span>
